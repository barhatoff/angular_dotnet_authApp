import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-auth-layer',
  imports: [RouterModule],
  templateUrl: './auth-layer.html',
})
export class AuthLayer {}
